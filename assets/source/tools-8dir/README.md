# 8-direction tools source assets (not shipped)

Preview GIFs and Aseprite sources for the held-item pack live here so Devvit
webview upload stays small. Runtime textures are the PNGs under
`public/harvest/sprites/` referenced by
`definingWorldPlazaHeldItemPresentationRegistry.ts`.

Do not put editor sources back under `public/` unless code starts loading them.
