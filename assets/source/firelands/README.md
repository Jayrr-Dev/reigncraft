# Firelands source assets (not shipped)

Original high-res sheets and unused tile/bubble packs live here so Devvit
webview upload stays small. Runtime textures are the resized PNGs under
`public/firelands/` referenced by
`registeringWorldPlazaTextureAssetManifest.ts`.

Do not put these files back under `public/` unless code starts loading them.
