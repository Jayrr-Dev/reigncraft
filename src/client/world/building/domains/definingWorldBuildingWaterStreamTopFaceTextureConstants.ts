/**
 * Visual constants for placed stream water top-face overlays.
 *
 * @module components/world/building/domains/definingWorldBuildingWaterStreamTopFaceTextureConstants
 */

/** Flow streak count per stream tile top face. */
export const DEFINING_WORLD_BUILDING_WATER_STREAM_TOP_FACE_TEXTURE_FLOW_STREAK_COUNT = 2;

/** Half-length of each flow streak in pixels. */
export const DEFINING_WORLD_BUILDING_WATER_STREAM_TOP_FACE_TEXTURE_FLOW_STREAK_HALF_LENGTH_PX = 7;

/** Stroke width for flow streaks in pixels. */
export const DEFINING_WORLD_BUILDING_WATER_STREAM_TOP_FACE_TEXTURE_FLOW_STREAK_WIDTH_PX = 0.9;

/** Alpha for flow streaks. */
export const DEFINING_WORLD_BUILDING_WATER_STREAM_TOP_FACE_TEXTURE_FLOW_STREAK_ALPHA = 0.42;

/** Highlight shimmer dot radius in pixels. */
export const DEFINING_WORLD_BUILDING_WATER_STREAM_TOP_FACE_TEXTURE_SHIMMER_RADIUS_PX = 1.6;

/** Alpha for highlight shimmer dots. */
export const DEFINING_WORLD_BUILDING_WATER_STREAM_TOP_FACE_TEXTURE_SHIMMER_ALPHA = 0.35;

/** Seeded unit above which a tile receives a shimmer dot. */
export const DEFINING_WORLD_BUILDING_WATER_STREAM_TOP_FACE_TEXTURE_SHIMMER_TILE_THRESHOLD = 0.68;

/** Salt for flow streak placement seeding. */
export const DEFINING_WORLD_BUILDING_WATER_STREAM_TOP_FACE_TEXTURE_FLOW_STREAK_SEED_SALT = 6511;

/** Salt for shimmer dot placement seeding. */
export const DEFINING_WORLD_BUILDING_WATER_STREAM_TOP_FACE_TEXTURE_SHIMMER_SEED_SALT = 7621;
