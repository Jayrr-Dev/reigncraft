/**
 * Infinite procedural map constants for the plaza sandbox.
 *
 * @module components/world/domains/definingWorldPlazaInfiniteMapConstants
 */

/** Extra tile rings drawn beyond the viewport edge. */
export const DEFINING_WORLD_PLAZA_INFINITE_MAP_VIEWPORT_PADDING_TILES = 2;

/** World spawn for a new session (origin). */
export const DEFINING_WORLD_PLAZA_INFINITE_MAP_SPAWN_X_PX = 0;

/** World spawn for a new session (origin). */
export const DEFINING_WORLD_PLAZA_INFINITE_MAP_SPAWN_Y_PX = 0;

/** Subtle third tile tone for procedural variation (every 7 tiles). */
export const DEFINING_WORLD_PLAZA_INFINITE_MAP_TILE_COLOR_ACCENT = 0x4a8a3c;

/** Modulus for accent tile placement. */
export const DEFINING_WORLD_PLAZA_INFINITE_MAP_TILE_ACCENT_MODULUS = 7;
