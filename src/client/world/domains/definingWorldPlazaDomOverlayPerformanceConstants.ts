/**
 * Main-thread budgets for non-critical DOM overlays.
 *
 * @module components/world/domains/definingWorldPlazaDomOverlayPerformanceConstants
 */

/** Maximum time one text-overlay callback may spend updating DOM each frame. */
export const DEFINING_WORLD_PLAZA_TEXT_OVERLAY_FRAME_BUDGET_MS = 0.75;
