/**
 * Viewport collision padding for slot-anchored inventory popovers.
 * Matches the authored `calc(100vw-2rem)` width gutter (1rem each side).
 */
export const DEFINING_WORLD_PLAZA_ANCHORED_POPOVER_VIEWPORT_EDGE_INSET_PX =
  16 as const;
