/**
 * Plot teleport landing tuning.
 *
 * @module components/world/domains/definingWorldPlazaPlotTeleportConstants
 */

/** Max Chebyshev tile distance from plot bounds for a plot teleport landing. */
export const DEFINING_WORLD_PLAZA_PLOT_TELEPORT_MAX_DISTANCE_FROM_BOUNDS_TILES = 2;

/** Min Chebyshev tile distance from plot bounds (1 keeps the landing outside the plot). */
export const DEFINING_WORLD_PLAZA_PLOT_TELEPORT_MIN_DISTANCE_FROM_BOUNDS_TILES = 1;

/** Default standing layer when resolving a plot teleport landing. */
export const DEFINING_WORLD_PLAZA_PLOT_TELEPORT_DEFAULT_LAYER = 1;
