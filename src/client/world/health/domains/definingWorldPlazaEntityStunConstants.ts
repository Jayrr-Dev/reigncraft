/** Default stun duration on the stun-debuff registry entry (ms). */
export const DEFINING_WORLD_PLAZA_STUN_DEFAULT_DURATION_MS = 4_000;

/** Max avatar body wobble in radians while stunned. */
export const DEFINING_WORLD_PLAZA_STUN_AVATAR_MAX_WOBBLE_RAD = 0.12;

/** Avatar wobble frequency in radians per second. */
export const DEFINING_WORLD_PLAZA_STUN_AVATAR_WOBBLE_FREQUENCY_RAD_PER_SEC = 7;

/** Number of gold dots orbiting above the stunned player's head. */
export const DEFINING_WORLD_PLAZA_STUN_DOT_COUNT = 4;

/** Orbit radius for stun dots in screen pixels before camera zoom. */
export const DEFINING_WORLD_PLAZA_STUN_DOT_ORBIT_RADIUS_PX = 14;

/** Diameter of each stun dot in screen pixels before camera zoom. */
export const DEFINING_WORLD_PLAZA_STUN_DOT_SIZE_PX = 5;

/** Orbit speed in full rotations per second. */
export const DEFINING_WORLD_PLAZA_STUN_DOT_ORBIT_ROTATIONS_PER_SEC = 0.9;

/** Extra offset above the avatar head for the stun dot orbit center (px, pre-zoom). */
export const DEFINING_WORLD_PLAZA_STUN_DOT_OFFSET_ABOVE_AVATAR_PX = 42;
