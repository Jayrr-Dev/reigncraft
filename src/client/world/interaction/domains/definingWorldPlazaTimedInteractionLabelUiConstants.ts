/** Row wrapper for a world-anchored action label with optional progress ring. */
export const DEFINING_WORLD_PLAZA_TIMED_INTERACTION_LABEL_ROW_CLASS_NAME =
  'pointer-events-auto relative inline-flex select-none items-center' as const;

/** Absolutely positioned slot for the progress ring right of the action label. */
export const DEFINING_WORLD_PLAZA_TIMED_INTERACTION_LABEL_RING_SLOT_CLASS_NAME =
  'absolute top-1/2 left-full -translate-y-1/2' as const;
