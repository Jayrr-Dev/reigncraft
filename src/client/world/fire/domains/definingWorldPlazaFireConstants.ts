/** Fire glow radius in isometric world-local pixels. */
export const DEFINING_WORLD_PLAZA_FIRE_GLOW_RADIUS_WORLD_LOCAL_PX = 56;

/** Warm core alpha multiplier for fire ground glow. */
export const DEFINING_WORLD_PLAZA_FIRE_GLOW_WARM_CORE_ALPHA = 0.62;

/** Floor-layer sort bias for fire warm glow sprites. */
export { DEFINING_WORLD_DEPTH_FIRE_GLOW_FLOOR_DEPTH_BIAS as DEFINING_WORLD_PLAZA_FIRE_GLOW_FLOOR_DEPTH_BIAS } from '@/components/world/depth';

/** Max fire glow sprites rendered per frame (viewport culling cap). */
export const DEFINING_WORLD_PLAZA_FIRE_GLOW_MAX_VISIBLE_COUNT = 24;
