/**
 * Shared inventory stack-size constants.
 *
 * @module components/inventory/domains/definingInventoryStackConstants
 */

/** Sentinel max stack size for items that never cap per slot. */
export const DEFINING_INVENTORY_UNLIMITED_STACK_SIZE = Number.POSITIVE_INFINITY;
