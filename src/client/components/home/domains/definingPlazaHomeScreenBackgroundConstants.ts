/**
 * Poster background art for splash / home sky scenes.
 * Vite `?url` keeps the asset in the client bundle upload path (same pattern
 * as the old mountain SVG), instead of relying only on `/home/*` public mirror.
 */
import reigncraftHomeBackgroundUrl from '../../../assets/home/reigncraft-home-bg.webp?url';

export const DEFINING_PLAZA_HOME_SCREEN_BACKGROUND_IMAGE_URL =
  reigncraftHomeBackgroundUrl;
